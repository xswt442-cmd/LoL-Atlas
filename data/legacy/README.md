# Legacy SQLite snapshots

These databases were copied byte-for-byte from the original LOL-set repository. They are retained as historical source material and have **not** been promoted to the validated `data/releases/<patch>/` contract.

Known issue: patch `16.15.1` contains temporary `Jade_*` champion variants and must be normalized before it can participate in canonical diffs. Consumers should use `data/releases/16.18.1/` for the current validated release.
