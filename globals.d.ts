/**
 * Injected by `build/hashed-data-asset.ts` via Vite's `define`. Holds the
 * content-addressed URL of the data snapshot (`/data/lol.<hash>.json`), so the
 * client fetches a path that is safe to cache immutably.
 */
declare const __LOL_DATA_URL__: string;
